# Web playground application

Owns WASM exports, playground evaluation options and JS-compatible result conversion. Browser capabilities are supplied by adapters/web. UI assets and browser acceptance scripts retain their existing owners.

```sh
./scripts/web/test-web-playground.sh
```

See [workspace architecture](../../docs/architecture.md) for dependency and lifetime boundaries.
