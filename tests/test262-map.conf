# Focused R2n admission profile for tests/test262-map.txt.
#
# BigInt, Map, Reflect.construct, Symbol, array-grouping, and arrow-function are
# inherited from the global fail-closed profile. Symbol.iterator and upsert are
# admitted only for this dependency-audited manifest. This scoped profile is
# evidence for the Map milestone, not a global support claim for tags whose Set,
# WeakMap, or other Test262 surfaces remain outside R2n.

[features]
BigInt
Map
Reflect.construct
Symbol
Symbol.iterator
array-grouping
arrow-function
upsert

[audited-negative-tests]
