# Runner-bound feature profile for tests/test262-weak-collections.txt.
#
# WeakMap, WeakSet, symbols-as-weakmap-keys, and upsert are admitted only for
# this metadata-derived cohort. This is focused milestone evidence, not global
# profile admission. Cross-realm and host-GC execution capabilities, plus the
# 29 TypedArray harness-unblocked audit paths, remain outside the gate.

[features]
Map
Reflect.construct
Set
Symbol
Symbol.iterator
Symbol.toStringTag
WeakMap
WeakSet
arrow-function
symbols-as-weakmap-keys
upsert

[audited-negative-tests]
# The selected cohort contains no negative-phase Test262 paths.
