# Focused R2s admission profile for tests/test262-object-binding.txt.
#
# This runner-bound profile admits only the dependency-audited object binding
# declaration cohort. The selected generated tests require no object-rest,
# assignment-pattern, parameter, catch-binding, generator, class-static-block,
# module, or async support.

[features]
destructuring-binding

[audited-negative-tests]
# The selected R2s manifest contains no negative-phase Test262 paths.
