# Scoped R3b profile for synchronous direct eval in or below a non-simple
# parameter environment.
#
# Async functions, generators, classes, accessors, modules, and the pinned
# QuickJS implicit-this known failure remain independent frontiers.

[features]
default-parameters

[audited-negative-tests]
