# Focused R2o admission profile for tests/test262-set.txt.
#
# The global profile now admits Set and set-methods. This runner-bound profile
# adds the exact well-known-symbol dependencies exercised by the frozen
# manifest. It is evidence for the strong Set milestone, not a global support
# claim for every well-known-symbol consumer, class, generator, WeakSet,
# cross-realm host, or other Test262 surface excluded from the gate.

[features]
Array.prototype.includes
BigInt
Reflect.construct
Set
Symbol
Symbol.iterator
Symbol.species
Symbol.toStringTag
arrow-function
set-methods

[audited-negative-tests]
