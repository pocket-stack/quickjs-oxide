# Focused R2q admission profile for tests/test262-array-binding-flat.txt.
#
# This runner-bound profile admits only flat identifier array bindings and
# their already-supported iterator dependency. It is not a global claim for
# nested patterns, object bindings, assignment patterns, parameter bindings,
# catch bindings, or generator/async contexts.

[features]
Symbol.iterator
destructuring-binding

[audited-negative-tests]
# The selected R2q manifest contains no negative-phase Test262 paths.
