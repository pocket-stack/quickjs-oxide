# R3di selection-only profile for the pinned non-blocking shared Atomics closure.
#
# This profile is valid only with tests/test262-shared-atomics-nonblocking.txt.
# The exact selection combines the 78-path SharedArrayBuffer-tagged projection
# with a disjoint 22-path source-audited spillover whose metadata omits that tag.
# It is evidence machinery, not a global feature admission or a waiter claim.

[features]
ArrayBuffer
Atomics
BigInt
DataView
Float32Array
Float64Array
Int8Array
Reflect.construct
SharedArrayBuffer
Symbol
Symbol.toPrimitive
TypedArray
Uint16Array
Uint8Array
Uint8ClampedArray
arrow-function
resizable-arraybuffer

[audited-negative-tests]
