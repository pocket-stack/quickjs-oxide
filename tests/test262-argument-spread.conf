# Focused R3d profile for argument spread in ordinary calls, method calls,
# direct eval, and construction.
#
# These are the only feature tags declared by the frozen 67-path cohort.
# Undeclared Proxy, class, generator, and newer-array dependencies remain
# visible as independently classified frontiers instead of widening the gate.

[features]
Symbol.iterator
json-parse-with-source

[audited-negative-tests]
