# Minimal scoped Test262 profile for the $262.createRealm activation.
# The feature set is the exact declared union for the 79-path activation plus
# the source-audited synthetic host admission tag.

[features]
BigInt
TypedArray
align-detached-buffer-semantics-with-web-reality
host-create-realm-required
host-gc-required
iterator-helpers

[audited-negative-tests]

[execution]
async=true
