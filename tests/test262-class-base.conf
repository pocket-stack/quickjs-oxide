# Focused R3e profile for the frozen base-class Test262 cohort.
#
# `class` remains deliberately absent from compat/test262-oxide.conf. This
# runner-bound profile admits it only for paths audited to avoid heritage,
# fields, private elements, generators, async methods, and static blocks.

[features]
Symbol
class

[audited-negative-tests]
# The selected base-class cohort contains no negative-phase metadata paths.
