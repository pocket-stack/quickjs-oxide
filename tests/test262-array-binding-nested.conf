# Focused R2r admission profile for tests/test262-array-binding-nested.txt.
#
# This runner-bound profile admits only recursively nested array bindings whose
# selected tests require no generator, object-pattern, assignment-pattern,
# parameter, catch-binding, module, or async support.

[features]
destructuring-binding

[audited-negative-tests]
# The selected R2r manifest contains no negative-phase Test262 paths.
