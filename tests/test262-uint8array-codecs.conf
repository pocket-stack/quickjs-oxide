# Checksum-bound capability profile for the focused Uint8Array base64/hex
# codec cohort. The global profile admits `uint8array-base64` only after this
# exact implementation slice and its pinned QuickJS differential pass.

[features]
Reflect.construct
TypedArray
uint8array-base64

[audited-negative-tests]

[execution]
async=true
