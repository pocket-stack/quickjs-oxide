# Minimal scoped Test262 profile for the $262.evalScript activation.
# The pinned direct-call cohort is otherwise featureless; this source-audited
# synthetic tag keeps the implemented hook out of the global vector.

[features]
host-eval-script-required

[audited-negative-tests]
